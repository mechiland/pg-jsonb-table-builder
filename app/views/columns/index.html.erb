<p style="color: green"><%= notice %></p>

<%= render @table %>

<table>
  <tr>
    <th>Name</th>
    <th>Code</th>
    <th>Type</th>
  </tr>
  <%= render @table.columns %>
</table>

<%= link_to "New column", new_table_column_path(@table) %>
