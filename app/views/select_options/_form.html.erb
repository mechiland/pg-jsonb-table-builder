<%= form_with(model: select_option) do |form| %>
  <% if select_option.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(select_option.errors.count, "error") %> prohibited this select_option from being saved:</h2>

      <ul>
        <% select_option.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :column_id, style: "display: block" %>
    <%= form.text_field :column_id %>
  </div>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_field :text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
